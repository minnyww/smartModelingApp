import { connect } from 'react-redux'
​
const mapStateToProps = () => {
  return {
  }
}
​
const mapDispatchToProps = () => {
  return {
  }
}
​
const PatternContainer = connect(mapStateToProps, mapDispatchToProps)(Link)
​
export default PatternContainer