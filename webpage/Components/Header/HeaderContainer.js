import { connect } from "react-redux";
import { View } from "../Header/HeaderView"
​
const mapStateToProps = () => {
  return {
  }
}
​
const mapDispatchToProps = () => {
  return {
  }
}
​
const HeaderContainer = connect(mapStateToProps, mapDispatchToProps)(Link)
​
export default HeaderContainer